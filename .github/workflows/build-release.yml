name: Build & Release .deb

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install build dependencies
        run: sudo apt-get install -y dpkg-dev

      - name: Build .deb package
        run: bash build-deb.sh

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Upload .deb as release asset
        uses: softprops/action-gh-release@v2
        with:
          name: "ÆRx Player ${{ github.ref_name }}"
          body: |
            ## Instalación

            Descarga el archivo `.deb` y ejecuta:

            ```bash
            sudo apt install ./aerx-player_${{ steps.version.outputs.VERSION }}_all.deb
            ```

            Consulta el [README](https://github.com/${{ github.repository }}#readme) para más información.
          files: dist/aerx-player_${{ steps.version.outputs.VERSION }}_all.deb
          draft: false
          prerelease: false
